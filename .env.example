# Copy this file to `.env` (never commit `.env`).

# Required — full API base URL including `/api` suffix.
# Local dev: Vite proxies browser requests from /api to this URL.
# Production: baked into the build at deploy time.
VITE_API_BASE_URL=https://your-api-host.example.com/api
